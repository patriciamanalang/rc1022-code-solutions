/* exported getLastChar */
// subtract 1 from the length of the string to get last index
// assign string at last index to obtain last character
function getLastChar(string) {
  var lastIndex = string.length - 1;
  var getLastChar = string[lastIndex];
  return getLastChar;

}
