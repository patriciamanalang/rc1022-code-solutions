/* exported drop */
// Create a function
// Create a variable  to hold the new array
// Slice the arrray at the designated count
// return the new array

function drop(array, count) {
  var newArray = array.slice(count);
  return newArray;
}
